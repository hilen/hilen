[package]
authors.workspace = true
description = "Proc macros for hilen physics levels."
edition.workspace = true
license.workspace = true
name = "level-proc"
repository.workspace = true
version.workspace = true

[lib]
proc-macro = true

[dependencies]
quote = { workspace = true }
syn = { workspace = true }

[lints]
workspace = true

[features]
# Makes `level` emit the ctor that registers a level test. Enabled through
# `hilen/level-tests`, never directly.
level-tests = []

# Makes `scene` emit the ctor that registers a scene test. Enabled through
# `hilen/scene-tests`, never directly.
scene-tests = []
