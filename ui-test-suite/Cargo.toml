[package]
edition.workspace = true
version.workspace = true

name = "ui-test-suite"

[features]
# The video playback test, on wherever a consumer turns `hilen/video` on.
video = ["hilen/video"]

[dependencies]
anyhow = { workspace = true }
log = { workspace = true }
parking_lot = { workspace = true }
pretty_assertions = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

hilen = { workspace = true, features = ["inspect", "level", "ui-tests"] }

[target.'cfg(target_arch = "wasm32")'.dependencies]
web-sys = { workspace = true }

[build-dependencies]
plat = { workspace = true }

[lints]
workspace = true
