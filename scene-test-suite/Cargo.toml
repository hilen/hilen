[package]
edition.workspace = true
version.workspace = true

name = "scene-test-suite"

[dependencies]
anyhow = { workspace = true }

hilen = { workspace = true, features = ["scene-tests"] }

[lints]
workspace = true
